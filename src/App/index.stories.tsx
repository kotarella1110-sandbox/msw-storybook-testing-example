import { ComponentMeta, ComponentStoryObj } from "@storybook/react";
import { App } from ".";

export default {
  component: App,
} as ComponentMeta<typeof App>;

export const Default: ComponentStoryObj<typeof App> = {};
